import type { Block } from 'payload'

export const StayDuration: Block = {
  slug: 'stayDuration',
  interfaceName: 'StayDurationBlock',
  fields: [
    // baseRate field removed
  ],
  labels: {
    singular: 'Stay Duration',
    plural: 'Stay Durations',
  },
} 